# PyTorch Geometric wheels — URL MUST match the torch + CUDA version.
# HSE cluster (module load Python/Google_Colab_GPU_2024): torch 2.1.2 + CUDA 12.1.
# Pinning a different torch/CUDA here causes torch-scatter/torch-sparse
# "undefined symbol" ABI errors at import — keep these in lockstep with the module.
--find-links https://data.pyg.org/whl/torch-2.1.2+cu121.html
torch-scatter
torch-sparse
torch-cluster
torch-geometric

# Core deep learning (provided by the cluster module; pin to match it)
torch==2.1.2
torchvision==0.16.2

# XAI attribution
captum

# Data & ML
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.2
scipy==1.13.1
joblib==1.4.2

# Visualisation
matplotlib==3.8.0
seaborn==0.13.2

tqdm==4.66.6
